# Core runtime. Install torch for your CUDA version first if needed:
# https://pytorch.org/get-started/locally/
torch>=2.2
torchvision>=0.17
accelerate>=0.30.0
pillow>=10.0.0
numpy>=1.24.0
tqdm>=4.66.0
pandas>=2.0.0
pycocotools>=2.0.7
huggingface_hub>=0.24.0

# Qwen3-VL support landed in recent Transformers builds. The official
# Qwen3-VL model card recommends installing Transformers from source.
git+https://github.com/huggingface/transformers
